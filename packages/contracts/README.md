# Contracts

Shared service contracts:

- `openapi/` - public REST API contract.
- `proto/` - internal gRPC contracts.
- `events/` - event envelope and payload schemas.

Run `make validate-contracts` to execute the Phase 0 validation tests.
