# Go Shared Packages

Reusable Go packages for configuration, event envelopes and service infrastructure.

- `config` - environment-backed service configuration and default service ports.
- `events` - shared event envelope parsing and validation.
- `servicehttp` - minimal HTTP server/runtime with base and health endpoints.
